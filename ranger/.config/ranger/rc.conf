# --- Minimal, macOS-safe rc.conf ---

default_linemode devicons


# Interface
set viewmode miller
set column_ratios 1,3,4
set show_hidden true
set draw_borders both
set mouse_enabled true
#set colorscheme default # default, solarized, jungle, snow
set colorscheme gruvbox
set confirm_on_delete multiple
set automatically_count_files true
set open_all_images true

# Preview behaviour
set use_preview_script true
set preview_script ~/.config/ranger/scope.sh
set preview_files true
set preview_directories true
set collapse_preview true

# IMPORTANT: disable inline image previews (avoid ImgDisplay crash)
set preview_images false
# Leave method unset. Uncomment ONLY if you run Kitty (not tmux):
# set preview_images_method kitty

# Quality of life
set sort natural
set sort_case_insensitive true
set sort_directories_first true
set save_console_history true
set autosave_bookmarks true
set save_tabs_on_exit false
set wrap_scroll false
set update_title false
set update_tmux_title true
set hint_collapse_threshold 10
set metadata_deep_search false

# Keys
default_linemode devicons
map o shell open %s          # open with default macOS app
map O shell open -R %f       # reveal in Finder

# zoxide
map gz console z
